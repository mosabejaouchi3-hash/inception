events {}

http {
    include /etc/nginx/mime.types;

    server {
        listen 4242;

        root /var/www/static;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}